import React from 'react';
import { mount } from 'enzyme';
import {{camelCaseName}} from '../src';

describe('<{{camelCaseName}} />', () => {
  test('renders correctly', () => {
    const wrapper = mount(<{{camelCaseName}} />);
    expect(wrapper).toMatchSnapshot();
  });
});
